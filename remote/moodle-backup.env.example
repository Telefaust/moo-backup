# Local overrides on the backup host (deployed by GUI or copy manually).
BACKUPER_LOCATION=/var/www/moodle
BACKUPER_STORAGE_PATH=/home/scripter/moobackup
# BACKUPER_LOGIN=scripter

# Optional: php binary for quiz_backup.php (default: /usr/bin/php or php from PATH)
# MOOBACKUP_QUIZ_PHP=/usr/bin/php8.2
